    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h3>Tipo de Prioridad</h3>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Prioridad</th>
                        
                    <th colspan="2">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.prioridad }}</td>
    <td><a href="{{ path('tipoprioridad_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('tipoprioridad_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('tipoprioridad')}}"><i class="fa fa-list"></i>
                Listado de Tipos de Prioridad
            </a>
        </li>
    </ul>


    {% endblock %}
